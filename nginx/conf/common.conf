# Inclus au niveau http (depuis /etc/nginx/conf.d/).
# Seul general.conf a sa place ici (map, gzip_types, ...).
# security.conf / headers.conf / proxy.conf / ssl.conf sont inclus
# depuis chaque vhost (server block), voir nginx/conf/template.conf.
include /etc/nginx/include/general.conf;
