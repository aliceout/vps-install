# HSTS: je te le mets mais “pas permanent” (pas includeSubDomains), pour éviter de te piéger
add_header Strict-Transport-Security "max-age=86400" always;

# CSP: à ajuster par service. Ici, rien (sinon tu vas te battre).
# add_header Content-Security-Policy "default-src 'self';" always;