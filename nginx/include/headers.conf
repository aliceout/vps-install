# Headers additionnels, a ajuster par service.
# HSTS, X-Frame-Options, nosniff, Referrer-Policy, Permissions-Policy
# sont deja dans security.conf (inclus via vhost-head.conf).
#
# Exemples a activer selon besoin du service :
# add_header Content-Security-Policy "default-src 'self';" always;
# add_header Cross-Origin-Opener-Policy "same-origin" always;
