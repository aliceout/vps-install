TYPE="native"
DESCRIPTION="Backup unifie : restic push vers home (VPS) OU borg/rotating/raid local (server)"

# Pas de secrets.tmpl pour la variante VPS : backup-run.sh fetch lui-meme
# depuis Infisical a chaque run (cle SSH ephemere, jamais ecrite sur disque).
# Variante server : aucun secret reseau, juste des configs locales.
