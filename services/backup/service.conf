TYPE="native"
DESCRIPTION="Backup restic vers home server (push ephemere via ssh-agent)"

# Pas de secrets.tmpl : les scripts fetchent eux-memes depuis Infisical a
# chaque run pour ne JAMAIS ecrire la cle privee SSH sur disque.
