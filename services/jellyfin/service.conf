TYPE="docker-compose"
DESCRIPTION="Jellyfin - media server (server uniquement)"

INFISICAL_PATH="/services/jellyfin"

# Pas de DATA_DIR uniforme : paths explicites dans le compose, lus depuis
# Infisical self-hosted (CONFIG_DIR + MEDIA_ROOT). RESTORE_ON_INSTALL ne
# s'applique pas (data hors arbre /home/<user>/data/).
