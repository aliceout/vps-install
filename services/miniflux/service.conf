TYPE="docker-compose"
DESCRIPTION="Miniflux - lecteur RSS minimaliste (Go + Postgres, data sur disque)"

INFISICAL_PATH="/services/miniflux"

# Data : Postgres uniquement (Miniflux est stateless, tout est en DB).
DATA_DIR="/home/$VPS_USER/data/miniflux"

RESTORE_ON_INSTALL=yes
