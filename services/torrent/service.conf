TYPE="docker-compose"
DESCRIPTION="Transmission via OpenVPN (torrent client, server uniquement)"

INFISICAL_PATH="/services/torrent"

# Data : path local arbitraire (peut etre sur un disque externe).
# Lu depuis Infisical self-hosted (DATA_DIR), pas dans /home/<user>/data/.
# RESTORE_ON_INSTALL ne s'applique pas (data hors arbre /home/<user>/data/).
