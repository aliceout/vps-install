TYPE="docker-compose"
DESCRIPTION="Transmission via Gluetun (WireGuard + Proton port-forward, home server)"

INFISICAL_PATH="/services/torrent"

# Data : path local arbitraire (peut etre sur un disque externe).
# Lu depuis Infisical Cloud sous /services/torrent/DATA_DIR, pas dans
# /home/<user>/data/. RESTORE_ON_INSTALL ne s'applique pas (data hors
# arbre /home/<user>/data/).
