TYPE="docker-compose"
DESCRIPTION="Vaultwarden - gestionnaire de mots de passe (compatible Bitwarden, SQLite bind-mountee)"

INFISICAL_PATH="/services/vaultwarden"

# Data : SQLite + attachments + sends + cles RSA, le tout dans /data (bind-mount).
DATA_DIR="/home/$VPS_USER/data/vaultwarden"

RESTORE_ON_INSTALL=yes
