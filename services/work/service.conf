TYPE="native"
DESCRIPTION="Work-resume (CV perso Next.js, deploye via pm2)"

# Pas d'INFISICAL_PATH : le service n'a pas de secrets applicatifs propres.
# Les secrets webhook (REPO/SECRET/SCRIPT) vivent sous /services/webhooks/work/.
